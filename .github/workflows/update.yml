name: Trigger COPR build

on:
  schedule:
    # Every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow placeholder
        run: |
          echo "This workflow is scheduled to run every 6 hours or on manual dispatch."
          echo "To auto-trigger COPR builds on schedule, add a step that POSTs your COPR custom webhook URL using a repository secret."
